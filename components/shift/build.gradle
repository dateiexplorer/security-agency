plugins {
    id 'java-library'
}

group 'de.dhbw.mosbach.msa'
version null

repositories {
    mavenCentral()
}

task build_jar(type: Jar) {
    project.archivesBaseName = 'shift'

    manifest {
        attributes["Main-Class"] = "Shift"
    }

    from { configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it )} }
    with jar
}